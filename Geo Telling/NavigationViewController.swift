import UIKit
import MapKit
import CoreLocation

class NavigationViewController: UIViewController {
 
	//This ViewController has some buttons on it that link to other ViewControllers (As can be seen in the Main.storyboard)
	
	override func viewDidLoad() {
		super.viewDidLoad()
	}
	
}
